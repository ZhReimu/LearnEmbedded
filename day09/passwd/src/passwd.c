#include <passwd.h>

/**
 * @brief 播放器处理函数
 * 
 * @param x 点击的 x 坐标
 * @param y 点击的 y 坐标
 */
void passwdHandler(int x, int y)
{
    debug2D("PasswdHandler %d, %d", x, y, INFO);
}
/**
 * @brief 初始化 点击区域
 * 
 */
void initPasswd()
{
    debug("Init Passwd", INFO);
}